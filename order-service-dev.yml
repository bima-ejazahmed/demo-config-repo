global:
  message: welcome to Order service dev environment
server:
  port: 8002
message: Hello from Order Dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_order_master_dev
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto:update
    show-sql:true
