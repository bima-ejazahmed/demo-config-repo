server:
  port: 8084
spring:
  application:
    name: demo-order-service
  datasource:
    url: jdbc:h2:mem:demo_order_master_dev
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
